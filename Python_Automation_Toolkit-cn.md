该脚本包含有助于减轻人类工作负荷的重要功能。
同时也帮助初学者入门使用Python。代码文档
在记事本中查看文件时，对齐正确。贾维斯被用作 谷歌助理。师傅

batch_file_rename.py - 该批次重命名给指定目录中的一组文件，
一旦你通过了当前和新的延伸。

create_dir_if_not_there.py - 检查用户中是否存在目录
家庭目录。如果不存在目录，则会创建一个目录。

快速YouTube下载器——带并行线程快速下载YouTube视频 使用Aria2C。

Google 图片下载器 - 查询给定词语并从 Google 检索图片 图片数据库。

dir_test.py - 测试是否存在目录 testdir，如果不存在，就会存在
如果你想创建目录，可以帮你创建。

env_check.py - 这个脚本会检查所有环境变量是否 必须设置。

blackjack.py - 本脚本包含Python版的Casino BlackJack-21游戏。

fileinfo.py - 显示给定文件的文件信息。

folder_size.py - 扫描当前目录及所有子目录和显示 尺寸。

logs.py - 该脚本将搜索给定目录中所有 *.log 文件，
用你指定的软件压缩，然后盖日期。

move_files_over_x_days.py - 将所有超过指定年龄（以天为单位）的文件从
源目录到目的目录。完美

nslookup_check.py - 这个简单的脚本会打开文件server_list.txt然后
对每个项目做NSLOOKUP来检查DNS条目。

osinfo.py - 显示你运行该程序的作系统的一些信息 剧本。

ping_servers.py - 根据提供的参数，该脚本会发出ping信号 与该应用组关联的服务器。

ping_subnet.py - 在提供前3个八位元组后，该文件扫描最终字节 可用地址的范围。

powerdown_startup.py - 该文件会通过服务器列表并ping到以下
机器，如果它开机了，它会加载腻子会话;如果不是，它会 通知你。

puttylogs.py - 这个文件会把给定目录里的所有日志压缩起来。

script_count.py - 该文件扫描脚本目录并给出计数 不同类型的剧本。

[get_youtube_view.py] - 这是一个简单的 Python 脚本，用于获得更多浏览量
你的YouTube视频。该脚本也可用于在YouTube上重复播放歌曲。

script_listing.py - 该文件将列出给定目录中的所有文件， 还要浏览所有子目录。

testlines.py - 这个简单的脚本打开一个文件，打印出100行
无论该线变量的集合是什么。

tweeter.py - 允许你在终端上发推文或图片。

serial_scanner.py包含一个叫 ListAvailablePorts 的方法，可以返回
列出计算机中正在使用的串口名称。就是这样 该方法仅在 Linux 和 Windows
上运行（可扩展支持 Mac OS）。如果没有端口 找到后返回一个空列表。

get_youtube_view.py - 一个简单的 Python 脚本，帮你为你的 YouTube
获得更多观看次数 视频。在YouTube上重复播放歌曲很有用。

CountMillionCharacter.py CountMillionCharacter2.0.py - 有字符数 一个文本文件。

xkcd_downloader.py - 下载最新的XKCD漫画并将它们放入新的漫画中
一个叫“comics”的文件夹。

timymodule.py - Python 'timeit' 模块的优秀替代品，使用更简单。

calculator.py - 使用 Python 的 eval（） 函数实现计算器。

Google_News.py - 使用BeautifulSoup提供最新新闻头条 新闻链接。

cricket_live_score - 使用BeautifulSoup提供现场板球比分。

youtube.py - 它会输入歌曲名称，并获取 最佳配对歌曲并演奏。

site_health.py - 检查远程服务器的健康状况

SimpleStopWatch.py - 使用 Python 时间的简单 Stop Watch 实现 模块。

Changemac.py - 这个脚本会更改你的MAC地址，生成随机的MAC地址
或者在你的Linux中输入新的MAC地址（已在Ubuntu中成功测试） 18.04).

whatsapp-monitor.py - 使用 Selenium 来显示联系人的在线状态
当你的联系人在华盛顿州上线时，你会收到更新 末期。

whatsapp-chat-analyzer.py - 这是Whatsapp群组/个人聊天分析器。 该脚本能够分析
WhatsApp 群组中发生的所有活动， 通过 matplotlib 库（图形式）可视化所有事物。

JARVIS.py - 用语音控制Windows程序。

图片下载器 - 下载图片表单网页 在基于 Unix 的系统上工作。

space_invader.py.py - 经典的太空侵略者2D游戏。回忆你的童年
通过玩经典的太空侵略者游戏来回忆。

测试用例生成器——生成不同类型的测试用例，具有干净的
友好的用户界面，用于竞技编程，甚至可能用于软件领域 测试。
